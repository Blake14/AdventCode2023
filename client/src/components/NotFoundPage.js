import React from "react";

const NotFoundPage = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100%",
        width: "100%",
        fontSize: 80,
        color: "#212529",
      }}
    >
      404: Page Not Found
    </div>
  );
};

export default NotFoundPage;
